name: homebase
version: "1.0.0"
config-version: 2
profile: homebase
model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  homebase:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
